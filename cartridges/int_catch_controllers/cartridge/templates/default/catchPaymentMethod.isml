<!--- TEMPLATENAME: catchPaymentMethod.isml --->

<iscomment>
    ***** Catch Payment </iscomment>

<iscomment>
    Left this div to avoid appearence of 'Your custom payment method integration goes here' message when Cath tab is selected
</iscomment>
<div class="payment-method <isif condition="${!empty(pdict.selectedPaymentID) && pdict.selectedPaymentID === 'Catch'}">payment-method-expanded</isif>" data-method="Catch">
</div>
