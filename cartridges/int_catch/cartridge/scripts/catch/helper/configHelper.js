'use strict';

/**
 * @description Getting preference value from custom config
 * @param {string} id - name of preference
 * @returns {*} - value of preference
 */
function getCustomConfigValue(id) { // eslint-disable-line no-unused-vars
    // Add the implementation of getting preference values from custom configuration
    return null;
}

module.exports = {
    getCustomConfigValue: getCustomConfigValue
};
